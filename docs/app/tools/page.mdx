export const metadata = {
  title: "Tools",
  description: "Configure web search, calculator, and URL reader tools — per-model and per-API-key settings in PullO.",
};

import { Callout } from "@/components/callout";

# Tools

<Callout variant="info" title="Coming soon">
  This section is under active development. Tool integrations are in early access.
</Callout>

Topics that will be covered here:

- Web search tool — enabling Tavily/Brave search for models
- Calculator tool — precise arithmetic for supported models
- URL reader tool — fetching and summarizing web pages
- Per-model tool configuration
- Per-API-key tool permission scoping
- Building custom tools
