export const metadata = {
  title: "MCP",
  description: "Connect MCP servers, build custom tools, and configure webhooks with PullO.",
};

import { Callout } from "@/components/callout";

# MCP

<Callout variant="info" title="Coming soon">
  This section is under active development. MCP (Model Context Protocol) support is currently in private beta.
</Callout>

Topics that will be covered here:

- Connecting MCP servers to PullO
- Exposing local MCP tools through the API
- Custom tool definitions and schemas
- Webhook configuration for async tool calls
- MCP server authentication and security
- Example: connecting a local filesystem MCP server
