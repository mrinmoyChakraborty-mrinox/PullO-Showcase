export const metadata = {
  title: "CLI & Agents",
  description: "Connect Claude Code, Cursor, Continue.dev, and LangChain to your local Ollama models through PullO.",
};

import { Callout } from "@/components/callout";

# CLI & Agents

<Callout variant="info" title="Coming soon">
  This section is under active development. Setup guides for popular coding agents and frameworks are being written.
</Callout>

Topics that will be covered here:

- **Claude Code** — setting `ANTHROPIC_BASE_URL` to PullO
- **Cursor** — configuring a custom OpenAI endpoint in Cursor settings
- **Continue.dev** — `config.json` setup for local models via PullO
- **LangChain** — using `ChatOpenAI` with a custom base URL
- **LlamaIndex** — OpenAI-compatible adapter configuration
- **Open WebUI** — connecting to PullO as a custom API endpoint
- Environment variable reference for common AI frameworks
